# Fast API Template

## How to run

```shell
poetry install
```

```shell
make start
```

## Update Code:

After making changes to the `openapi/schema.yml` you can regenerate the code:

```shell
make openapi
```